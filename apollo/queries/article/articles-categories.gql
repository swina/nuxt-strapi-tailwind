query Category($id: ID!){
  category(id: $id) {
    name
    articles {
      id
      slug
      title
      content
      image {
        url
      }
      category {
        id
        name
      }
    }
  }
}